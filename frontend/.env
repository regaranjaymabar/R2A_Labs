# Konfigurasi Alamat URL Server Backend API (MySQL / Express / Laravel / dll)
# Catatan: Di Vite, variabel wajib diawali dengan VITE_ agar bisa diakses via import.meta.env
VITE_API_URL=http://localhost:3000
